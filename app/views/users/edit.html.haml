- if @user.errors.any?
  #error_explanation
    %h2
      = pluralize(@user.errors.count, "error")
      prohibited this article from being saved:
    %ul
      - @user.errors.full_messages.each do |msg|
        %li= msg

%h1 Edit user
= form_for @user do |u|
  %div= u.email_field    :email
  %div= u.submit 'Save'